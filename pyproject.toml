[project]
name = "pytorch-learning"
version = "0.1.0"
description = "Beginner-friendly PyTorch learning project."
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
